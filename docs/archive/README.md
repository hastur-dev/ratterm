# Archive

Planning documents for work that has since landed. Kept because they record why
a subsystem is shaped the way it is, which the code cannot say for itself.

They are not instructions. Nothing here should be executed or treated as an
open task list.

| File | Describes | Landed as |
|---|---|---|
| `plan-theming-and-terminal-selection.md` | The theme system and terminal mouse selection | `src/theme/`, `src/terminal/` |
| `upgrade-phases.md` | Git integration, the debugger, the daemon, Docker logs | `src/git/`, `src/debugger/`, `src/daemon/`, `src/docker_logs/` |

For what the code looks like now, read `docs/architecture.md`.
