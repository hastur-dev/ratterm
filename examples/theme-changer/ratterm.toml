[extension]
name = "theme-changer"
version = "1.0.0"
description = "Cycles through available themes or sets a specific theme"
author = "Ratterm Examples"

[process]
command = "python"
args = ["{ext_dir}/main.py"]
cwd = "{ext_dir}"
restart_on_crash = false
max_restarts = 0
